services:
  - type: web
    name: idempotency-gateway
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "run:app" --workers 4 --threads 2 --bind 0.0.0.0:10000
    plan: free